import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class RtlService {
  // TODO: تبديل السمة dir="rtl|ltr" على عنصر <html>
  // TODO: تحديث اتجاه التخطيط ديناميكياً عند تغيير اللغة
}